# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=Hashed

# API
NEXT_PUBLIC_API_URL=http://localhost:3000/api
REQRES_API_KEY=your_reqres_api_key

# Feature flags (optional)
NEXT_PUBLIC_ENABLE_ANALYTICS=false

# Authentication (generate with: node -e "console.log(require('node:crypto').randomBytes(32).toString('hex'))")
AUTH_SECRET=replace_with_a_random_64_character_secret
