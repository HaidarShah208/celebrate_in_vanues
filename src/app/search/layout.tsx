/**
 * Search is a full-viewport results experience — the marketing footer from the
 * root layout is intentionally omitted here.
 */
export default function SearchLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}
